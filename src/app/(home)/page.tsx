import { Wrapper } from "@/components/ui/wrapper";

export default function HomePage() {
  return (
    <main className="flex-1">
      <Wrapper className="w-full h-full flex justify-center items-center text-zinc-500 text-lg">
        Create a new list of tasks on the left side
      </Wrapper>
    </main>
  );
}
